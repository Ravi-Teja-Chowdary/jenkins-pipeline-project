pipeline {
     agent any
     stages {
         stage('Testing') {
              steps {
                 echo "Welcome to Jenkins Pipeline Testing"
                 sh "ls -al"
              }
         }
     }
}
